import {
  GET_SHARED,
  ADD_SHARED,
  DELETE_SHARED,
  SET_LOADING,
  SHARE_ERROR
} from './types';

// set loading to true
export const setLoading = () => {
  return {
    type: SET_LOADING
  };
};

// get shared items

// add shared item

// remove item from shared list
